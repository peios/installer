# One source release, split along privilege and lifecycle boundaries.

format = "peipkg"

[package]
version       = "{{version}}-1"
architecture  = "x86_64"
license       = "MIT AND Apache-2.0 AND Unicode-3.0 AND Zlib"
license_class = "free"
homepage      = "https://github.com/peios/installer"
